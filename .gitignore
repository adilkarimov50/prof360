# Environment & secrets
.env
.env.local
.env.*.local

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
backend/.venv/
.pytest_cache/
.mypy_cache/
htmlcov/
.coverage

# Node
frontend/node_modules/
frontend/dist/
*.tsbuildinfo

# Docker & data volumes
pgdata/
uploads/
backend/uploads/
backend/data/legal/documents/*.docx
backend/data/legal/documents/*.txt
backend/data/legal/documents/cache_meta.json

# ML caches
.hf_cache/
backend/.hf_cache/

# OS & IDE
.DS_Store
.idea/
.vscode/
*.swp
*.swo

# Build artifacts
build/
dist/
*.log

# Large local Excel (keep passports/data JSON)
*.xlsx
*.xls
!passports/data/**/*.json
# Выгрузки, на которые ссылается опубликованная справка
!passports/docs/**/*.xlsx

# Local working docs (not in repo)
/*.docx
/*.pdf
/*.html
/Карасайский район/
/Илийский район/
/ИИ_порноконтент_блокировка/
/Комиссия*/
/Уйгур*/
/.matplotlib/

# Восстановленный проект «Туран» (скоринг) — исходники и дамп держим локально
/turan/
/Туран */
*.7z

# Оперативные дашборды с персональными данными (не публиковать)
passports/private/

# Сырые выгрузки социалки / ЦКС (Excel с ИИН)
/социалка за область /

# Cursor
.cursor/

# Legacy nested git (removed after monorepo merge)
passports/.git/

# Временные файлы Word (создаются при открытом документе)
~$*
